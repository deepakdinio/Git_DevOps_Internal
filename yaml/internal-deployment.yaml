apiVersion: apps/v1
kind: Deployment
metadata:
  name: Internal-deployment
spec:
  selector:
    matchLabels:
      app: Internal
  template:
    metadata:
      labels:
        app: Internal
    spec:
      containers:
      - name: Internal
        image: deepakdinio/git_devop_internal1:2
        resources:
          requests:
            memory: "32Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
